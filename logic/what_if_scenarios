# The "What If" List (Edge Cases)

* **What if** the customer is in a different country when the glass breaks?
* **What if** the customer accidentally uploads a photo of their dog instead of their car?
* **What if** the car is a super-expensive Ferrari? (Maybe we shouldn't let the robot pay that automatically!)
* **What if** the repair shop is closed?
